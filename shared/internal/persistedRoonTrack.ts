type PersistedRoonTrack = {
  trackId: string;
  albumId: string;
  roonTrackName: string;
  roonNumber: string;
  roonPosition: number;
  createdAt: string;
  updatedAt: string;
};

export type { PersistedRoonTrack };
